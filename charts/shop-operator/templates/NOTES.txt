shop-operator has been installed.

Check the manager is running:
  kubectl get deployment -n {{ .Release.Namespace }} {{ include "shop-operator.fullname" . }}

CRDs installed by this release:
  kubectl get crd shops.apps.shophub.io wallets.apps.shophub.io discordchannels.apps.shophub.io

NOTE: this chart no longer holds the Discord bot's credentials itself — it reads
{{ .Values.discord.secretName }} live from the {{ .Values.discord.secretNamespace }} namespace
(the shophub chart's own Secret) on every reconcile. If that Secret doesn't exist yet (shophub
chart installed without discord.botToken/existingSecret set), the DiscordChannel controller
runs fine but won't be able to create shop alert channels until it does.
